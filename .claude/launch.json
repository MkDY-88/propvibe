{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "propvibe-local",
      "runtimeExecutable": ".venv/Scripts/python.exe",
      "runtimeArgs": [
        "-c",
        "import os, uvicorn; uvicorn.run('main:app', host='127.0.0.1', port=int(os.environ.get('PORT', '39811')))"
      ],
      "port": 39811,
      "autoPort": true
    }
  ]
}
